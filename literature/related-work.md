# Related Work

> This file is for narrative survey writing. For structured paper data, see `papers.yaml`.
> 
> Run `uv run python scripts/setup.py` to generate a customized template, or write manually below.

---

## Overview

*Describe the research landscape and where your work fits.*

## 1. Core Methods

*Survey the foundational methods your work builds on.*

See `papers.yaml` for structured references. Cite as `[@key]` in experiment READMEs.

## 2. Related Approaches

*What else has been tried? How does your approach differ?*

## 3. Our Positioning

*What gap does this project fill? What's the key differentiator?*

---

## References

Papers are tracked in `papers.yaml`. Run `python scripts/lit_index.py` to see the citation index.
